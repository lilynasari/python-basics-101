"""
功能：在1-10中随机挑选一个整数并给用户三次机会猜测该数字，猜测错误会提示相比答案大了还是小了
输入：用户猜测的数字最多输入三次
输出：用户猜测的结果：猜测正确或者猜大了或者猜小了，并在用户三次机会用完后提示你失败了
"""

def guess_numbers():
    import random
    num = random.randint(1, 10)
    a = int(input("please input the number:"))
    if a > num:
        print("the number you guessed is greater than the number")
        b = int(input("one more chance:"))
        if b > num:
            print("the number you guessed is greater than the number")
            b = int(input("one more chance:"))
            if b > num:
                print("u failed")
            elif b < num:

                print("u failed")
            else:
                print("u got the right number")
        elif b < num:
            print("the number you guessed is less than the number")
            b = int(input("one more chance:"))
            if b > num:
                print("u failed")
            elif b < num:
                print("u failed")
            else:
                print("u got the right number")
        else:
            print("u got the number")
    elif a < num:
        print("the number you guessed is less than the number")
        b = int(input("one more chance:"))
        if b > num:
            print("the number you guessed is greater than the number")
            b = int(input("one more chance:"))
            if b > num:
                print("u failed")
            elif b < num:
                print("u failed")
            else:
                print("u got the right number")
        elif b < num:
            print("the number you guessed is less than the number")
            b = int(input("one more chance:"))
            if b > num:
                print("u failed")
            elif b < num:
                print("u failed")
            else:
                print("u got the right number")
        else:
            print("u got the number")
    else:
        print("u got the right number")
if __name__ == "__main__":
    guess_numbers()
